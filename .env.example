# FlowForge environment variables (optional overrides for local development)
# Copy to .env and adjust values as needed.

ConnectionStrings__DefaultConnection=Server=localhost;Database=FlowForge;Trusted_Connection=True;TrustServerCertificate=True;
Jwt__Issuer=FlowForge
Jwt__Audience=FlowForge
Jwt__Secret=FlowForge-Dev-Secret-Key-At-Least-32-Chars!
Jwt__ExpiryMinutes=60
